<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.edu.xmut.learningplatform.mapper.previewMapper">

    <insert id="addPreView" useGeneratedKeys="true" keyProperty="id" parameterType="preview">
        INSERT INTO preview (name, startTime, endTime, chapterId)
        VALUES (#{name}, #{startTime}, #{endTime}, #{chapterId})
    </insert>

    <select id="getPreViewByChapterId" resultType="preview">
        select * from preview where chapterId = #{chapterId}
    </select>

</mapper>